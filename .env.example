# Copy this file to `.env` at the repo root. Used by docker-compose.
# Do NOT commit the real `.env`.

POSTGRES_USER=internflow_user
POSTGRES_PASSWORD=internflow_password
POSTGRES_DB=internflow_tracker
POSTGRES_PORT=5432
