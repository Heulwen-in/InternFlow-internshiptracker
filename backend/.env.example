DATABASE_URL="postgresql://internflow_user:internflow_password@localhost:5432/internflow_tracker"
JWT_SECRET=""
PORT=5000
CORS_ORIGIN="http://localhost:5173"
APP_URL="http://localhost:5173"

# Local Ollama job-description parser (no hosted API key required).
OLLAMA_BASE_URL="http://127.0.0.1:11434"
OLLAMA_MODEL="qwen2.5:3b"
OLLAMA_TIMEOUT_MS=30000

# SMTP email settings for OTP verification and password reset.
# Gmail example: SMTP_HOST="smtp.gmail.com", SMTP_PORT=587, SMTP_SECURE=false,
# SMTP_USER="your-email@gmail.com", SMTP_PASS="your-gmail-app-password"
SMTP_HOST=""
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=""
SMTP_PASS=""
MAIL_FROM="InternFlow <no-reply@internflow.local>"